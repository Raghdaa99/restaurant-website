<template>
  <div class="bg-gradient-to-r from-primary to-secondary text-white text-center p-10 rounded-lg">
    <h2 class="text-2xl font-bold">{{ $t("call_to_action") }} </h2> 
    <p class="mt-2">{{ $t("call_to_action_description") }}</p>
    <button class="mt-4 bg-white text-primary px-6 py-2 rounded-md font-semibold">{{ $t("order_now") }}</button>
  </div>
</template>